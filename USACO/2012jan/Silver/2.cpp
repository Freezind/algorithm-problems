// USACO 2012 January Contest, Silver Division
// Problem 2. Bale Share
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=107
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("baleshare.in", "r", stdin);
    freopen("baleshare.out", "w", stdout);

    return 0;
}
