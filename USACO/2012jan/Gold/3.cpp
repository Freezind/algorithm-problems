// USACO 2012 January Contest, Gold Division
// Problem 3. Bovine Alliance
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=111
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("alliance.in", "r", stdin);
    freopen("alliance.out", "w", stdout);

    return 0;
}
