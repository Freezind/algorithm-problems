// USACO 2013 March Contest, Silver
// Problem 3. The Cow Run
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=264
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("cowrun.in", "r", stdin);
    freopen("cowrun.out", "w", stdout);

    return 0;
}
