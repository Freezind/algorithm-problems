// USACO 2012 February Contest, Silver Division
// Problem 2. Cow IDs
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=116
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("cowids.in", "r", stdin);
    freopen("cowids.out", "w", stdout);

    return 0;
}
