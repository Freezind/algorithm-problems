// USACO 2014 March Contest, Silver
// Problem 1. Watering the Fields
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=415
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("irrigation.in", "r", stdin);
    freopen("irrigation.out", "w", stdout);

    return 0;
}
