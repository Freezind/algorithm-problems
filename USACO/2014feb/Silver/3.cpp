// USACO 2014 February Contest, Silver
// Problem 3. Secret Code
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=399
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("scode.in", "r", stdin);
    freopen("scode.out", "w", stdout);

    return 0;
}
