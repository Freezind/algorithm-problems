// USACO 2012 US Open, Gold Division
// Problem 1. Tied Down
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=137
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("tied.in", "r", stdin);
    freopen("tied.out", "w", stdout);

    return 0;
}
