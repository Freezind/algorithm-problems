// USACO 2011 November Contest, Bronze Division
// Problem 3. Moo Sick
// link: http://www.usaco.org/index.php?page=viewproblem2&cpid=86
// status: unsolved
// tag:

#include <bits/stdc++.h>

#define loop(i, l, r) for(int i = (l); i < (r); ++i)
#define pool(i, l, r) for(int i = (l); i > (r); --i)
#define debug(x) cerr << #x << ": " << x << endl;
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    freopen("moosick.in", "r", stdin);
    freopen("moosick.out", "w", stdout);

    return 0;
}
